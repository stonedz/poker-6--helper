name: Build Windows EXE

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build-windows-exe:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          pip install -e .[windows-capture]

      - name: Build single-file executable (PyInstaller)
        run: |
          pyinstaller --noconfirm --onefile --name shortdeck-cli --add-data "src/shortdeck_cli/data/preflop_scenarios.json;shortdeck_cli/data" src/shortdeck_cli/__main__.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: shortdeck-cli-windows-exe
          path: dist/shortdeck-cli.exe
